"use client";
import { useDemoData } from "./data-context";
export function FilterBar(){const {filters,setFilter,reset,data}=useDemoData();const filtered=Object.values(filters).filter(Boolean).length;return <section className="filter-bar" aria-label="Global data filters"><div className="demo-badge"><i/>Demo Data</div><label>Quarter<select value={filters.quarter} onChange={e=>setFilter("quarter",e.target.value)}><option value="all">All quarters</option>{data.quarters.map(q=><option key={q}>{q}</option>)}</select></label><label>From<input type="date" value={filters.startDate} onChange={e=>setFilter("startDate",e.target.value)}/></label><label>To<input type="date" value={filters.endDate} onChange={e=>setFilter("endDate",e.target.value)}/></label><label>Contractor<select value={filters.contractorId} onChange={e=>setFilter("contractorId",e.target.value)}><option value="">All contractors</option>{data.contractors.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Carrier<select value={filters.carrierId} onChange={e=>setFilter("carrierId",e.target.value)}><option value="">All carriers</option>{data.carriers.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>State<select value={filters.state} onChange={e=>setFilter("state",e.target.value)}><option value="">All states</option>{data.states.map(x=><option key={x}>{x}</option>)}</select></label><label>Service<select value={filters.serviceType} onChange={e=>setFilter("serviceType",e.target.value)}><option value="">All services</option><option value="estimate_only">Estimate only</option><option value="claim_handling">Claim handling</option></select></label><button className="filter-reset" onClick={reset} disabled={!filtered}>Reset</button></section>}
