import { Brand } from "@/components/brand";
import { signIn } from "@/app/auth/actions";

export default async function Login({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){
  const params=await searchParams;
  return <main className="login-page"><section className="login-story"><Brand/><div><span className="eyebrow"><i className="live-dot"/> STAGING ACCESS</span><h1>From submitted file<br/>to measurable outcome.</h1><p>One trusted view of estimating activity, claim handling, additional RCV, cycle time, and operational throughput.</p></div><div className="login-signal"><small>CONTROLLED ENVIRONMENT</small><strong>C2</strong><span>authenticated staging</span><em>Accounts are created by an administrator</em></div></section><section className="login-form-wrap"><form className="login-form" action={signIn}><input type="hidden" name="next" value={params.next??"/dashboard"}/><div><small>AUTHORIZED USERS</small><h2>Sign in to TotalScope</h2><p>Use the staging account issued by an administrator.</p></div>{params.error&&<p role="alert" className="live-data-error">{params.error}</p>}<label>Work email<input name="email" type="email" autoComplete="email" required/></label><label>Password<span><input name="password" type="password" autoComplete="current-password" required/></span></label><button type="submit" className="button login-button">Sign in <span>→</span></button><div className="divider"><span>staging only</span></div><button type="button" className="sso-button" disabled aria-disabled="true"><b>◆</b> Enterprise SSO — not configured</button><p className="form-foot">No public sign-up. Production authentication rollout is not yet complete.</p></form></section></main>
}
